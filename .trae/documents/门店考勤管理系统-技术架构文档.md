## 1. 架构设计

本系统采用纯前端架构，使用 React 构建单页应用（SPA），所有业务数据采用 Mock 数据模拟，便于前端开发和演示。

```mermaid
flowchart TB
    subgraph "前端应用层"
        A["React 应用 (Vite)"]
        A1["路由管理 (React Router)"]
        A2["状态管理 (Context + useState)"]
        A3["UI 组件库"]
        A4["图表库 (Recharts)"]
    end
    
    subgraph "数据层"
        B["Mock 数据层"]
        B1["门店数据"]
        B2["员工数据"]
        B3["排班数据"]
        B4["打卡数据"]
        B5["审批数据"]
        B6["异常工单数据"]
    end
    
    subgraph "样式层"
        C["TailwindCSS 3"]
        C1["全局主题配置"]
        C2["响应式设计"]
        C3["动画与过渡"]
    end
    
    A --> A1
    A --> A2
    A --> A3
    A --> A4
    A --> B
    A --> C
```

## 2. 技术描述

- **前端框架**：React 18 + TypeScript
- **构建工具**：Vite 5
- **样式方案**：TailwindCSS 3
- **路由管理**：React Router v6
- **图表库**：Recharts
- **图标库**：Lucide React
- **状态管理**：React Context + Hooks
- **日期处理**：date-fns
- **Mock 数据**：本地 JSON 数据 + 工具函数生成

## 3. 路由定义

| 路由路径 | 页面名称 | 说明 |
|----------|----------|------|
| `/dashboard` | 门店看板 | 系统首页，展示考勤概览数据 |
| `/schedule` | 员工班表 | 排班管理页面 |
| `/checkin` | 移动打卡记录 | 打卡记录与核验页面 |
| `/exceptions` | 异常工单 | 异常工单管理页面 |
| `/leave` | 请假调班 | 请假与调班申请页面 |
| `/approval` | 审批中心 | 审批待办与历史页面 |
| `/summary` | 总部汇总 | 数据汇总与统计页面 |

## 4. 数据模型

### 4.1 数据模型定义

```mermaid
erDiagram
    STORE ||--o{ EMPLOYEE : "拥有"
    STORE ||--o{ SCHEDULE : "有排班"
    EMPLOYEE ||--o{ SCHEDULE : "被排班"
    EMPLOYEE ||--o{ CHECKIN_RECORD : "有打卡"
    EMPLOYEE ||--o{ LEAVE_REQUEST : "提交"
    EMPLOYEE ||--o{ SWAP_REQUEST : "提交"
    EMPLOYEE ||--o{ EXCEPTION_TICKET : "关联"
    LEAVE_REQUEST ||--o{ APPROVAL_RECORD : "有审批"
    SWAP_REQUEST ||--o{ APPROVAL_RECORD : "有审批"
    EXCEPTION_TICKET ||--o{ APPROVAL_RECORD : "有审批"
    EMPLOYEE ||--o{ ATTENDANCE_SUMMARY : "有统计"
    
    STORE {
        string id PK "门店ID"
        string name "门店名称"
        string address "门店地址"
        string manager "店长"
        string lat "纬度"
        string lng "经度"
        number employeeCount "员工数"
        date createdAt "创建时间"
    }
    
    EMPLOYEE {
        string id PK "员工ID"
        string storeId FK "所属门店"
        string name "姓名"
        string position "职位"
        string phone "手机号"
        string avatar "头像"
        string status "状态"
        date joinDate "入职日期"
        number annualLeaveDays "年假天数"
        number sickLeaveDays "病假天数"
        number compLeaveDays "调休天数"
    }
    
    SCHEDULE {
        string id PK "排班ID"
        string employeeId FK "员工ID"
        string storeId FK "门店ID"
        date date "日期"
        string shiftType "班次类型"
        string startTime "开始时间"
        string endTime "结束时间"
        boolean isSupport "是否跨店支援"
        string supportStoreId "支援门店ID"
        string note "备注"
    }
    
    CHECKIN_RECORD {
        string id PK "打卡记录ID"
        string employeeId FK "员工ID"
        string storeId FK "门店ID"
        date date "日期"
        string checkInTime "上班打卡时间"
        string checkOutTime "下班打卡时间"
        string photo "打卡照片"
        string location "打卡地点"
        number distance "距离(米)"
        string status "打卡状态"
        boolean isDistanceAbnormal "距离异常"
        boolean isPhotoAbnormal "照片异常"
    }
    
    LEAVE_REQUEST {
        string id PK "请假申请ID"
        string employeeId FK "员工ID"
        string storeId FK "门店ID"
        string leaveType "请假类型"
        date startDate "开始日期"
        date endDate "结束日期"
        number days "天数"
        string reason "原因"
        string status "状态"
        date createdAt "提交时间"
    }
    
    SWAP_REQUEST {
        string id PK "调班申请ID"
        string applicantId FK "申请人ID"
        string targetId FK "调换人ID"
        string storeId FK "门店ID"
        date applicantDate "申请人日期"
        date targetDate "调换人日期"
        string applicantShift "申请人班次"
        string targetShift "调换人班次"
        string reason "原因"
        string status "状态"
        date createdAt "提交时间"
    }
    
    EXCEPTION_TICKET {
        string id PK "工单ID"
        string employeeId FK "员工ID"
        string storeId FK "门店ID"
        string type "异常类型"
        date date "异常日期"
        string description "描述"
        string priority "优先级"
        string status "状态"
        string appeal "申诉内容"
        date createdAt "创建时间"
    }
    
    APPROVAL_RECORD {
        string id PK "审批记录ID"
        string sourceId FK "来源ID"
        string sourceType "来源类型"
        string approverId FK "审批人ID"
        string approverRole "审批人角色"
        string result "审批结果"
        string comment "审批意见"
        date createdAt "审批时间"
    }
    
    ATTENDANCE_SUMMARY {
        string id PK "统计ID"
        string employeeId FK "员工ID"
        string storeId FK "门店ID"
        number year "年份"
        number month "月份"
        number workDays "应出勤天数"
        number actualDays "实际出勤天数"
        number lateCount "迟到次数"
        number earlyLeaveCount "早退次数"
        number absentCount "缺勤次数"
        number leaveDays "请假天数"
        number bonus "奖金"
        number fine "罚款"
        boolean isLocked "是否锁账"
    }
```

### 4.2 核心数据结构

```typescript
// 门店
interface Store {
  id: string;
  name: string;
  address: string;
  manager: string;
  lat: number;
  lng: number;
  employeeCount: number;
  createdAt: string;
}

// 员工
interface Employee {
  id: string;
  storeId: string;
  name: string;
  position: string;
  phone: string;
  avatar: string;
  status: 'active' | 'inactive' | 'blacklist';
  joinDate: string;
  annualLeaveDays: number;
  sickLeaveDays: number;
  compLeaveDays: number;
}

// 班次类型
type ShiftType = 'morning' | 'middle' | 'evening' | 'rest' | 'custom';

// 排班
interface Schedule {
  id: string;
  employeeId: string;
  storeId: string;
  date: string;
  shiftType: ShiftType;
  startTime: string;
  endTime: string;
  isSupport: boolean;
  supportStoreId?: string;
  note?: string;
}

// 打卡状态
type CheckinStatus = 'normal' | 'late' | 'early_leave' | 'absent' | 'pending';

// 打卡记录
interface CheckinRecord {
  id: string;
  employeeId: string;
  storeId: string;
  date: string;
  checkInTime?: string;
  checkOutTime?: string;
  photo: string;
  location: string;
  distance: number;
  status: CheckinStatus;
  isDistanceAbnormal: boolean;
  isPhotoAbnormal: boolean;
}

// 异常类型
type ExceptionType = 'late' | 'early_leave' | 'absent' | 'distance' | 'photo' | 'other';

// 异常工单
interface ExceptionTicket {
  id: string;
  employeeId: string;
  storeId: string;
  type: ExceptionType;
  date: string;
  description: string;
  priority: 'high' | 'medium' | 'low';
  status: 'pending' | 'processing' | 'resolved' | 'rejected';
  appeal?: string;
  createdAt: string;
}

// 请假类型
type LeaveType = 'annual' | 'sick' | 'personal' | 'compensation' | 'maternity' | 'other';

// 请假申请
interface LeaveRequest {
  id: string;
  employeeId: string;
  storeId: string;
  leaveType: LeaveType;
  startDate: string;
  endDate: string;
  days: number;
  reason: string;
  status: 'pending' | 'approved' | 'rejected';
  createdAt: string;
}

// 调班申请
interface SwapRequest {
  id: string;
  applicantId: string;
  targetId: string;
  storeId: string;
  applicantDate: string;
  targetDate: string;
  applicantShift: ShiftType;
  targetShift: ShiftType;
  reason: string;
  status: 'pending' | 'approved' | 'rejected';
  createdAt: string;
}

// 审批记录
interface ApprovalRecord {
  id: string;
  sourceId: string;
  sourceType: 'leave' | 'swap' | 'exception';
  approverId: string;
  approverRole: 'store_manager' | 'hr';
  result: 'approved' | 'rejected';
  comment: string;
  createdAt: string;
}

// 考勤汇总
interface AttendanceSummary {
  id: string;
  employeeId: string;
  storeId: string;
  year: number;
  month: number;
  workDays: number;
  actualDays: number;
  lateCount: number;
  earlyLeaveCount: number;
  absentCount: number;
  leaveDays: number;
  bonus: number;
  fine: number;
  isLocked: boolean;
}
```

## 5. 项目结构

```
src/
├── assets/           # 静态资源
├── components/       # 公共组件
│   ├── Layout/       # 布局组件
│   ├── Card/         # 卡片组件
│   ├── Table/        # 表格组件
│   ├── Modal/        # 弹窗组件
│   └── ...
├── pages/            # 页面组件
│   ├── Dashboard/    # 门店看板
│   ├── Schedule/     # 员工班表
│   ├── Checkin/      # 移动打卡记录
│   ├── Exceptions/   # 异常工单
│   ├── Leave/        # 请假调班
│   ├── Approval/     # 审批中心
│   └── Summary/      # 总部汇总
├── data/             # Mock 数据
│   ├── stores.ts
│   ├── employees.ts
│   ├── schedules.ts
│   ├── checkins.ts
│   └── ...
├── hooks/            # 自定义 Hooks
├── utils/            # 工具函数
├── types/            # TypeScript 类型定义
├── context/          # Context 状态
├── App.tsx
├── main.tsx
└── index.css
```

## 6. 核心功能实现方案

### 6.1 排班功能
- 使用日历网格展示排班，支持月视图和周视图切换
- 班次以不同颜色色块展示，支持拖拽调整
- 提供班次模板侧边栏，可快速分配班次
- 实现班次复制功能，支持复制上周/上月排班

### 6.2 打卡核验
- 打卡记录表格展示，支持按日期、门店、状态筛选
- 照片核验弹窗，展示打卡大图、位置信息、距离数据
- 距离异常自动标记，红色高亮展示
- 支持缺勤登记和补卡申请

### 6.3 审批流程
- 审批中心分为待审批、已审批标签页
- 支持请假、调班、异常工单三类审批
- 实现店长初审 → 人事复核二级审批
- 支持批量审批操作

### 6.4 数据统计
- 使用 Recharts 实现柱状图、折线图、饼图
- 门店排行榜按出勤率排名
- 考勤奖扣自动计算，支持手动调整
- 月末锁账功能，锁定后数据只读
